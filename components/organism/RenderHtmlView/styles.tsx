import {StyleSheet} from 'react-native';
import {WINDOW_WIDTH} from '@gorhom/bottom-sheet';

export default StyleSheet.create({
  container: {},
  defaultImgTagStyle: {maxWidth: WINDOW_WIDTH - 32},
});

import Colors from '@constants/colors';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {},
  uploadedQuestionText: {fontSize: 18, marginBottom: 14},
  uploadedMoreSwipeupContainer: {
    marginTop: 12,
    marginHorizontal: 16,
  },
  uploadedMoreSwipeupText: {
    fontSize: 16,
    margin: 12,
    color: Colors.dark.neutral100,
  },
});

import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {},
  buttonContainer: {
    marginTop: 24,
    flexDirection: 'row',
    flexWrap: 'wrap',
    // gap: 10,
  },
});
